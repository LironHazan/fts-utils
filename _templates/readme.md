Globally install hygen

Generate the constraint boilerplates by running:
```` hygen mygen new  --name compy````

The files will be generated at:
```Loaded templates: _templates
       added: app/compy.ts
       added: app/compy.interface.ts
```
